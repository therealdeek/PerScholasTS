const originalArray = [1, 2, 5, 10, 40];
const multiplyByTen = originalArray.map((element, index, arr) => `This number is ${element} at ${index}`);
console.log(multiplyByTen);
export {};
//# sourceMappingURL=types.js.map